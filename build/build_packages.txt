# Packages required only by the build environment to produce the initrd
# These are NOT installed inside the initrd image
# Version history:
# 0.1.0 - Initial split from packages.txt; previously hardcoded in workflow apt-get line
# 0.1.1 - Removed localepurge (Debian-only; belongs in initrd_packages.txt not on Ubuntu runner)
binfmt-support
cpio
debootstrap
dpkg
gzip
initramfs-tools
qemu-user-static
rsync
xz-utils
zstd
